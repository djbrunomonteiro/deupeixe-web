export interface IUser{
    id: string,
    name?: string | null,
    email?: string | null,
    plan?: string | null,
    photo? : string | null,
    fone?: string | null,
    city?: string | null,
    country?: string | null,
    state?: string | null,
}